@AGENTS.md

# steel-order-system

製造業（鋼板切断部門）向けの受注管理システム。
紙伝票とExcelに分散した受注情報を一元化し、転記作業をなくすことを目的とする。

## コメントの方針

作者が学習中のため、生成したコードには処理の意味がわかるコメントを入れること。
ポートフォリオとして公開する際に整理するので、現時点では詳しめで構わない。

## 表現上の制約

ポートフォリオとして公開するため、実在する企業・製品・個人が特定できる名称は
コード・コメント・ドキュメントのいずれにも含めないこと。

## 設計書

実装前に必ず該当する設計書を読むこと。

- `docs/basic-design.md` — スコープ、業務フロー、機能仕様、権限設計
- `docs/table-design.md` — テーブル定義、リレーション、制約、RLS方針
- `docs/screen-design.md` — 画面レイアウト、入力挙動

## 技術スタック

TypeScript / React / Next.js (App Router) / Supabase / Vercel

## 実装方針

- 権限制御はクライアント側の表示制御だけでなく、Supabase の RLS でも強制する
- 残数量・重量区分など計算で求められる値はカラムに保持しない
- 価格ルールはコードの条件分岐ではなく価格マスタのデータとして持つ
- 対象解像度は 1280×1024 固定。レスポンシブ対応は行わない

## 開発の進め方

- マイグレーションは Supabase の SQL Editor で手動実行する。
  Claude Code は実DBに適用できないため、SQLファイルの生成までを行う。
- スキーマ変更後は `npm run db:types` で型定義を再生成する。
- 新しいテーブルを作成したら、Supabase の Data API 設定で公開する必要がある
  （自動公開をオフにしているため）。
- 機能ごとにブランチを切り、PR を経て main にマージする。

## 設計変更

- 設計書に記載のない判断が必要になった場合は、実装前に確認する。
- 実装が設計書と食い違った場合は、設計書側も更新する。
- マイグレーションは追加していく。既存ファイルは書き換えない。

## 開発状況

認証・マスタ管理（価格体系の再設計を含む）を実装済み。
次は価格マスタの初期データ投入と、単価計算ロジックの実装に着手する。